import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';

export type <%= pascalCaseName %>Document = <%= pascalCaseName %> & Document;

@Schema({
  toJSON: {
    transform(doc, ret) {
      ret.id = ret._id.toString();

      delete ret._id;
      delete ret.__v;

      return ret;
    },
  }
})
export class <%= pascalCaseName %> {
  @Prop({ required: true })
  name: string;
}

export const <%= pascalCaseName %>Schema = SchemaFactory.createForClass(<%= pascalCaseName %>);
